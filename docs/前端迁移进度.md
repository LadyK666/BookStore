## 网上书店前端迁移进度记录（JavaFX → Web）

> 目标：在**不改变业务逻辑**的前提下，将原有 JavaFX 前端完整迁移为 Web 前端（React + Ant Design），并在视觉与交互上进行现代化优化。

---

### 一、整体架构

- **旧前端**：`com.bookstore.ui.*`（`MainApp` / `LoginView` / `CustomerView` / `AdminView`），JavaFX 桌面应用。
- **新前端**：`web-frontend`（Vite + React + TypeScript + Ant Design），运行于浏览器。
- **后端**：保留原有 `dao` / `service` / `model`，新增 `com.bookstore.web.*` 中的 REST Controller，复用全部业务逻辑与 JDBC 访问。

路由结构（前端）：

- `/login` → 登录页（顾客 / 管理员）
- `/customer/*` → 顾客端工作台（浏览 / 下单 / 订单 / 账户）
- `/admin/*` → 管理后台（库存 / 采购 / 订单 / 发货 / 客户 / 供应商 / 书目）

---

### 二、登录功能（LoginView → Web）

- **后端接口**
  - `POST /api/auth/login` → `AuthController.login()`  
    - 逻辑完全对应 `LoginView.handleLogin()`：
      - 管理员：固定账号 `admin / admin123`。
      - 顾客：`CustomerDao.findByUsername` + 密码校验 + 账户状态校验。
  - `POST /api/auth/register` → `AuthController.register()`  
    - 逻辑对应 `LoginView.showRegisterDialog()` 中的注册流程（检查重名、插入默认信用等级等，简化了部分可选字段）。

- **前端实现**
  - 文件：`src/pages/LoginPage.tsx`
  - 状态与逻辑：
    - 用户类型切换（顾客 / 管理员）。
    - 登录成功后：
      - 顾客 → 路由跳转 `/customer`，并带上 `customerId` / `customerName`。
      - 管理员 → 路由跳转 `/admin`，并带上 `adminName`。
    - “注册新账号（顾客）”按钮调用 `/api/auth/register`。
  - **UI 优化**：渐变背景 + 居中卡片 + 中英文标题，相比 JavaFX 版本更现代。

**迁移状态**：✅ 已完成（逻辑等价，UI 优化）。

---

### 三、顾客端（CustomerView → `/customer`）

#### 3.1 布局与基础信息

- **旧逻辑**：`CustomerView` 构造函数中：
  - 加载顾客信息与信用等级：`loadCustomerInfo()`。
  - 创建三部分 UI：顶部导航、左侧书目浏览、右侧购物车、底部个人信息。

- **后端接口**
  - `GET /api/customer/{customerId}/summary` → `CustomerProfileController.summary()`  
    - 使用 `CustomerDao` + `CreditLevelDao`。
    - 返回：账户余额、信用等级 ID/名称、折扣系数、权限说明文本（`getCreditPrivilegeText` 的等价实现）。

- **前端实现**
  - 文件：`src/pages/customer/CustomerLayout.tsx`
  - 页面结构：
    - **Header**：应用标题、欢迎语（真实姓名）、按钮（我的订单 / 通知 / 退出登录）。
    - **左侧 Sider**：书目浏览（搜索 + 列表）。
    - **右侧 Content**：购物车与应付金额。
    - **底部 Footer**：账户余额 + 信用等级 + 权限说明。

**迁移状态**：✅ 已完成（信息展示逻辑与折扣计算完全等价）。

#### 3.2 书目浏览与搜索（loadAllBooks / searchBooks）

- **旧逻辑**：`CustomerView.loadAllBooks()` 与 `searchBooks(String keyword)`：
  - 展示所有在售书目。
  - 搜索规则：将关键字在**书号 / 书名 / 出版社 / 作者名 / 关键字文本**上模糊匹配。

- **后端接口**
  - `GET /api/customer/books` → `CustomerBookController.listAll()` → `BookDao.findAll()`
  - `GET /api/customer/books/search?keyword=...` → `CustomerBookController.search()`：
    1. `BookDao.findByConditions(kw, kw, kw)`：按书号 / 书名 / 出版社模糊匹配。
    2. `AuthorDao.findBookIdsByAuthorNameLike(kw)`：按作者名模糊匹配。
    3. `KeywordDao.findBookIdsByKeywordTextLike(kw)`：按关键字文本模糊匹配。
    4. **增强**：在所有书目的 ISBN / 标题 / 出版社 上再做一次不区分大小写的匹配，确保“把关键字放进所有主要属性做匹配”的体验。
    5. 使用 `LinkedHashMap` 去重并保留顺序，与桌面端一致。

- **前端实现**
  - 书目列表列结构与行为：
    - 书号（点击弹出详情）/ 书名 / 出版社 / 定价 / 状态 / 操作(加入购物车)。
  - 搜索输入框：
    - 输入任意关键字（如 `B`），会被上传到上述接口，按照所有属性进行匹配；符合条件的书目全部展示。

**迁移状态**：✅ 已完成（搜索范围更宽，但兼容旧逻辑）。

#### 3.3 书目详情（showBookDetailDialog）

- **旧逻辑**：点击表中的书号，弹出对话框展示书目完整信息。

- **前端实现**
  - 文件：`CustomerLayout.tsx` 内的书目详情 `Modal`。
  - 使用 Ant Design `Descriptions` 展示：
    - 书号 / ISBN / 书名 / 出版社 / 出版日期 / 版次 / 定价 / 状态 / 目录（多行文本）。

**迁移状态**：✅ 已完成（字段和内容与旧前端对齐，UI 更美观）。

#### 3.4 购物车与折扣计算（refreshCartTable 等价逻辑）

- **旧逻辑**：`CustomerView.refreshCartTable()` 中：
  - 单价 = 书目定价 × 当前顾客信用等级折扣。
  - 小计 = 单价 × 数量。
  - 总金额为所有小计之和。

- **前端实现**
  - 购物车表格：书名 / 数量 / 小计 / 删除。
  - “应付金额”：
    - 在前端使用 `summary.discountRate` 对单价进行折扣计算，对应 JavaFX 的折后价逻辑。

**迁移状态**：✅ 已完成（加入购物车与折扣计算逻辑等价，下单流程待迁移）。

#### 3.5 “我的订单”与订单详情 + 付款 / 收货确认（showMyOrders / showOrderDetail / showReceiveDialog）

- **旧逻辑**：`CustomerView.showMyOrders()`：
  - 顶部状态筛选（全部 / PENDING_PAYMENT / ...）。
  - 表格展示订单列表。
  - 针对每个订单可查看详情、付款、确认收货。
  - 详情中展示订单明细与发货记录。

- **后端接口**
  - `GET /api/customer/{customerId}/orders?status=...` → `CustomerOrderController.listOrders()`  
    - 调用 `SalesOrderDao.findByCustomerId(customerId)` 再按状态过滤，完全对应 JavaFX 逻辑。
  - `GET /api/customer/orders/{orderId}` → `CustomerOrderController.orderDetail()`  
    - `SalesOrderDao.findById` + `findItemsByOrderId`。
    - `ShipmentDao.findByOrderId`。
  - `POST /api/customer/orders/{orderId}/pay` → `CustomerOrderController.pay()`  
    - 直接调用 `OrderService.payOrder(orderId)`，与 JavaFX 中按钮逻辑一致。
  - `POST /api/customer/orders/{orderId}/receive` → `CustomerOrderController.confirmReceive()`  
    - 调用 `ShipmentService.confirmReceipt(orderId, receiveMap)`，参数为 `orderItemId → 本次确认收货数量` 的 Map。
    - 内部使用 `SalesOrderDao` / `ShipmentDao` 校验“已发未收”的剩余数量，并分摊到各发货明细，和 `showReceiveDialog` 一致。

- **前端实现**
  - 顶部导航中的“**我的订单**”按钮（代替 JavaFX 顶部按钮）。
  - 点击弹出一个大弹窗：
    - 顶部：AntD `Select` 状态筛选（全部 / PENDING_PAYMENT / OUT_OF_STOCK_PENDING / PENDING_SHIPMENT / DELIVERING / SHIPPED / COMPLETED / CANCELLED），选中即自动触发筛选请求。
    - 中间：订单列表表格，订单号可点击。
      - 列中包含“付款”按钮（仅在 `PENDING_PAYMENT/OUT_OF_STOCK_PENDING` 时可点击），点击调用 `/orders/{orderId}/pay`。
      - 列中包含“确认收货”按钮（仅在 `DELIVERING/SHIPPED` 状态可用），点击后弹出收货确认对话框。
    - 下方：被选中订单的详情区域：
      - 顶部文字：状态 / 下单时间 / 应付金额。
      - “订单明细”表：书号 / 订购数量 / 成交单价 / 小计 / 已发货数量 / 已收货数量。
      - “发货信息”表：发货单号 / 快递公司 / 快递单号 / 发货时间。
  - 收货确认对话框：
    - 表格列出当前订单中“已发货但未全部收货”的明细行。
    - 每行展示：书号 / 订购数量 / 已发货 / 已收货 / 本次确认收货数量（可编辑 `InputNumber`，上限为剩余可收货数量）。
    - 点击“确认收货”后，将 `orderItemId → 本次收货数量` 的 Map 发送到 `/receive` 接口，成功后刷新订单列表与订单详情。

**迁移状态**：✅ “我的订单”列表、详情、付款与收货确认流程已在 Web 端完成迁移，业务逻辑与 JavaFX 等价。

#### 3.6 收货地址管理（showAddressManagement / loadAddresses / showAddAddressDialog）

- **旧逻辑**：`CustomerView.showAddressManagement()`：
  - 弹出地址管理对话框，展示当前顾客的所有收货地址。
  - 支持“新增地址”“设为默认”“删除”三种操作。

- **后端接口**
  - `GET /api/customer/{customerId}/addresses` → `CustomerAddressController.list()`  
    - 使用 `CustomerAddressDao.findByCustomerId` 返回当前顾客全部地址，默认地址排在最前。
  - `POST /api/customer/{customerId}/addresses` → `CustomerAddressController.add()`  
    - 使用 `CustomerAddressDao.insert` 新增地址；当 `isDefault=true` 时，会先将该顾客其他地址设为非默认，与原逻辑一致。
  - `POST /api/customer/{customerId}/addresses/{addressId}/default` → `CustomerAddressController.setDefault()`  
    - 调用 `CustomerAddressDao.updateDefault`，确保同一顾客仅有一个默认地址。
  - `DELETE /api/customer/{customerId}/addresses/{addressId}` → `CustomerAddressController.delete()`  
    - 调用 `CustomerAddressDao.delete` 删除指定地址。

- **前端实现**
  - 顾客页底部新增“**管理地址**”按钮（对应 JavaFX 底部按钮）。
  - 点击后弹出“收货地址管理”弹窗：
    - 表格展示收件人、电话、拼接好的完整地址、是否默认等信息。
    - 每行支持：
      - “设为默认”操作：调用 `/default` 接口。
      - “删除”操作：带二次确认的删除按钮，调用 `DELETE` 接口。
    - 弹窗底部有“新增地址”按钮，弹出 AntD 表单：
      - 字段：收件人、电话、省、市、区/县、详细地址、是否设为默认。
      - 与 `showAddAddressDialog` 的校验逻辑一致：收件人和详细地址为必填。

**迁移状态**：✅ 地址列表 + 新增 + 设为默认 + 删除 功能已完成；❌ 在下单流程中选择地址快照的 Web 化仍待迁移（对应 `chooseShippingAddressSnapshot`）。

#### 3.7 下单基础流程 + 缺书登记决策（submitOrder / showOutOfStockRequestDialog / createCustomerRequestsAndOutOfStockRecords）

- **旧逻辑**：`CustomerView.submitOrder()`：
  - 根据购物车中图书与顾客当前信用等级折扣，计算每行折后价与订单总金额。
  - 选择收货地址快照。
  - 创建订单主表与明细，初始状态为 `PENDING_PAYMENT`。
  - **缺书检查**：遍历订单明细，检查每本书的当前库存是否满足订购数量；如存在缺书项，弹出 `showOutOfStockRequestDialog` 对话框，让顾客选择：
    - **方案一**：付款并自动生成缺书记录（调用 `OrderService.payOrder`，内部会生成正式缺书记录）。
    - **方案二**：仅提交缺书登记（暂不付款），订单状态改为 `OUT_OF_STOCK_PENDING`，等待管理员决策。
  - 无论选择哪种方案，都会调用 `createCustomerRequestsAndOutOfStockRecords` 写入 `customer_out_of_stock_request` 表。
  - 如无缺书项，直接提示顾客在“我的订单”中付款。

- **后端接口**
  - `POST /api/customer/{customerId}/orders` → `CustomerOrderController.createOrder()`  
    - 使用 `CustomerDao`、`CreditLevelDao` 读取顾客与信用等级，按折扣计算折后价与总金额。
    - 使用 `SalesOrderDao.createOrder` 创建订单主表与明细。
    - 将订单状态设置为 `PENDING_PAYMENT`，返回创建后的订单。
  - `GET /api/customer/orders/{orderId}/shortages` → `CustomerOrderController.listShortages()`  
    - 遍历订单明细，调用 `InventoryDao.getQuantity` 检查每本书的当前库存；返回库存不足的明细列表（`ShortageItemResp`：`orderItemId / bookId / quantity / currentStock`）。
  - `POST /api/customer/orders/{orderId}/shortages/decision` → `CustomerOrderController.handleShortageDecision()`  
    - 请求体：`{ decision: "PAY_AND_CREATE" | "REQUEST_ONLY", customerNote?: string }`。
    - 重新计算缺书明细，调用 `createCustomerRequestsAndOutOfStockRecords`（等价于 JavaFX 同名方法）：
      - 若 `decision=PAY_AND_CREATE`：先创建正式缺书记录（`OutOfStockRecordDao.insert`，source=CUSTOMER_REQUEST），再调用 `OrderService.payOrder(orderId)` 完成付款（内部会更新累积消费、自动升级信用等级、处理缺货待确认订单的自动生成缺书记录）。
      - 若 `decision=REQUEST_ONLY`：仅写入 `customer_out_of_stock_request`（processed_status=PENDING），并将订单状态改为 `OUT_OF_STOCK_PENDING`。
    - 返回更新后的订单对象。

- **前端实现**
  - 顾客页面右侧购物车区域“提交订单”按钮：
    - 请求体中包含购物车中的书号、数量与原始定价。
    - 地址快照优先使用默认地址或唯一地址；如暂未维护地址，则退回到真实姓名，行为与桌面端一致的简化版。
  - **缺书登记交互**（对应 JavaFX `showOutOfStockRequestDialog`）：
    - 下单成功后，自动调用 `/customer/orders/{orderId}/shortages` 检查缺书项。
    - 如存在缺书项，弹出 `ShortageModal` 弹窗：
      - 展示缺书明细表格（书号 / 订购数量 / 当前库存）。
      - 提供备注输入框（对应 `customerNote`）。
      - 两个按钮：
        - **“仅提交缺书登记（暂不付款）”**：调用 `/shortages/decision`，`decision=REQUEST_ONLY`，成功后提示“缺书登记已提交，订单状态已标记为【缺货待确认】”。
        - **“付款并生成缺书记录”**：调用 `/shortages/decision`，`decision=PAY_AND_CREATE`，成功后提示“缺书登记已生成并完成付款，等待发货”。
    - 如无缺书项，直接提示“订单创建成功，请在【我的订单】中完成付款”。
  - 下单/缺书登记完成后：
    - 清空购物车。
    - 刷新账户余额与“我的订单”列表（信用等级可能已自动升级）。

**迁移状态**：✅ 下单流程与缺书登记决策功能已完成迁移，包括缺书检查、决策对话框、缺书记录生成与订单状态流转，业务逻辑与 JavaFX 完全等价。**信用等级自动升级**（根据累积消费在付款时自动检查并升级）已通过 `OrderService.payOrder` 内置逻辑自动执行，无需前端额外处理。

#### 3.8 账户充值与个人信息修改（showRechargeDialog / showEditCustomerInfoDialog）

- **旧逻辑**：
  - `showRechargeDialog()`：弹出充值对话框，支持几个固定金额与自定义金额，确认后更新账户余额并提示当前余额。
  - `showEditCustomerInfoDialog()`：弹出表单对话框，允许修改真实姓名、手机号、邮箱，包含非空和邮箱格式校验。

- **后端接口**
  - `POST /api/customer/{customerId}/recharge` → `CustomerProfileController.recharge()`  
    - 校验金额 > 0，读取顾客当前余额，调用 `CustomerDao.updateAccountBalance` 增加余额。
    - 返回更新后的 `ProfileResp`，包含最新余额与信用等级信息。
  - `PUT /api/customer/{customerId}/profile` → `CustomerProfileController.updateProfile()`  
    - 校验真实姓名非空、邮箱格式（包含 `@`）。
    - 调用 `CustomerDao.updateCustomerInfo` 更新真实姓名、手机号、邮箱，并返回更新后的 `ProfileResp`。

- **前端实现**
  - 充值：
    - 在顾客页面底部余额统计旁增加“充值”按钮，点击弹出 AntD `Modal`，包含固定金额按钮与自定义金额输入框。
    - 确认后调用 `/recharge` 接口，成功时刷新余额并提示“当前余额：¥XXX”。
  - 修改个人信息：
    - 在顶部欢迎区域增加“修改信息”按钮，点击弹出 AntD 表单 `Modal`。
    - 表单字段：用户名（只读）、真实姓名、手机号、邮箱；校验规则与 JavaFX 版本一致。
    - 保存后调用 `/profile` 接口，刷新内存中的 `summary` 信息。

**迁移状态**：✅ 账户充值与个人信息编辑功能已完成，行为与桌面端一致，UI 采用了更现代的表单与弹窗样式。 

---

#### 3.9 消息通知列表（showOutOfStockNotificationList / notifyOutOfStockDecisions）

- **旧逻辑**：
  - `CustomerView.showOutOfStockNotificationList()`：顶部“通知”按钮点击后，弹出表格对话框展示当前顾客的所有通知记录。
  - `CustomerView.notifyOutOfStockDecisions()`：在打开“我的订单”前自动检查是否有未通知的缺书登记处理结果，如有则自动写入 `customer_notification` 表（type=OUT_OF_STOCK，标题“缺书登记处理结果”，内容为通过/未通过提示）。
  - 通知来源包括：缺书登记处理结果（管理员通过/拒绝）、部分发货提醒等，数据来自 `customer_notification` 表。

- **后端接口**
  - `GET /api/customer/{customerId}/notifications` → `CustomerNotificationController.listNotifications()`  
    - 直接复用 `CustomerNotificationDao.findByCustomerId(customerId)`，按创建时间倒序返回全部通知。
  - **缺书登记处理结果通知**：由 `AdminPurchaseController.acceptCustomerRequest()` / `rejectCustomerRequest()` 在管理员处理顾客缺书登记时自动写入通知记录（见 5.4 采购管理章节）。

- **前端实现**
  - 顶部 Header 中保留小铃铛“通知”按钮（与 JavaFX 顶部按钮语义一致）：  
    - 点击后打开“消息通知”弹窗。
  - 弹窗内部使用 AntD `Table` 展示：  
    - 类型（type）/ 订单号（orderId）/ 标题（title）/ 内容（content）/ 时间（createdTime）。  
  - 数据来源：`GET /api/customer/{customerId}/notifications`，完全对应 JavaFX 中对 `CustomerNotificationDao.findByCustomerId` 的使用。
  - **注意**：JavaFX 中的 `notifyOutOfStockDecisions` 自动检查逻辑在 Web 端已由后端在管理员处理时直接写入通知，无需前端主动轮询；顾客打开通知列表即可看到所有历史通知（包括缺书登记处理结果）。

**迁移状态**：✅ 消息通知列表功能已完成迁移，展示字段与顺序对齐桌面端，实现等价的数据来源与交互。缺书登记处理结果通知已由后端在管理员处理时自动写入，与 JavaFX 的自动检查逻辑等价，只在 UI 上做了现代化视觉优化。

---

### 四、尚未迁移的顾客端功能（计划）

以下功能仍在迁移计划中，将严格按照 `CustomerView` 中的实现迁移到 Web：

1. **收货地址在下单流程中的选择与快照**：
   - 逻辑参考：`chooseShippingAddressSnapshot()`。
   - 目前下单时已支持“自动选择默认地址/唯一地址或真实姓名”，但尚未提供类似 JavaFX 的弹窗式地址选择器（允许顾客在下单时从多个地址中选择一个作为本次订单的收货地址快照）。
2. 其它零散提示信息与通知展示样式微调。

---

### 五、管理员端（AdminView → `/admin`）

管理员端功能较多，整体仍按模块逐步迁移，目前**订单管理模块已完成首批迁移**，其余模块处于接口梳理阶段。

#### 5.1 订单管理（showOrderManagement / showOrderDetailForAdmin）

- **旧逻辑**：`AdminView.showOrderManagement()` / `setupOrderTable()` / `showOrderDetailForAdmin()`  
  - 左侧菜单“订单管理”选中后，在中间内容区展示订单列表。  
  - 顶部提供状态筛选（全部 / 各种状态），点击“筛选”按钮刷新。  
  - 表格列：订单号（可点击查看详情）、客户ID（可点击查看客户详情）、下单时间、状态、金额。  
  - 点击订单号打开订单详情对话框，展示：订单基本信息、明细列表、发货记录。

- **后端接口**
  - `GET /api/admin/orders?status=...` → `AdminOrderController.listOrders()`  
    - 使用 `SalesOrderDao.findAll()` / `findByStatus(status)` 查询所有订单或按状态过滤。  
    - 返回字段与 JavaFX 中 `SalesOrder` 表格列完全一致：`orderId / customerId / orderTime / orderStatus / payableAmount / shippingAddressSnapshot` 等。  
  - `GET /api/admin/orders/{orderId}` → `AdminOrderController.orderDetail()`  
    - 与顾客端 `CustomerOrderController.orderDetail()` 类似：  
      - `SalesOrderDao.findOrderById(orderId)` 获取订单主信息。  
      - `SalesOrderDao.findItemsByOrderId(orderId)` 获取订单明细。  
      - `ShipmentDao.findByOrderId(orderId)` 获取发货记录。  
    - 返回 `OrderDetailResp`：`order + items + shipments`，供管理端详情页使用。

- **前端实现**
  - 文件：`src/pages/admin/AdminLayout.tsx`  
  - 布局结构：  
    - 顶部 `Header`：系统标题“书店管理后台”、当前管理员姓名、退出按钮（返回登录页），对应 JavaFX 顶部栏。  
    - 左侧 `Sider`：菜单项（订单管理 / 发货管理 / 库存管理 / 采购管理 / 客户管理 / 供应商管理 / 书目管理），当前仅“订单管理”实现，其余标记为“待迁移”。  
    - 右侧 `Content`：在选择“订单管理”时显示订单列表和筛选区域。  
  - 订单列表：  
    - 使用 `Table` 展示：订单号（点击可查看详情）、客户ID、下单时间、状态、金额。  
    - 顶部使用 `Select` 实现状态筛选（全部 / PENDING_PAYMENT / OUT_OF_STOCK_PENDING / PENDING_SHIPMENT / DELIVERING / SHIPPED / COMPLETED / CANCELLED），改变选项时自动调用 `/admin/orders?status=...`。  
  - 订单详情弹窗：  
    - 顶部使用 `Descriptions` 展示订单号、客户ID、状态、下单时间、应付金额、收货地址快照。  
    - “订单明细”表格：书号 / 数量 / 成交单价 / 小计 / 明细状态。  
    - “发货记录”表格：发货单号 / 快递公司 / 快递单号 / 发货时间 / 发货状态 / 操作员。  
    - 打开方式：点击订单列表中的订单号。  

**迁移状态**：✅ 管理员端“订单管理”列表与详情已完成迁移，数据源与筛选逻辑完全复用原 DAO，展示字段与 JavaFX 保持一致，UI 使用 Ant Design 做了后台风格的整体布局。其余管理模块（发货 / 库存 / 采购 / 客户 / 供应商 / 书目）仍按计划逐步迁移中。

#### 5.2 发货管理（showShipmentManagement / showShipDialog / showPartialShipDialog）

- **旧逻辑**：`AdminView.showShipmentManagement()`  
  - 列表展示待发货/配送中/（信用允许的）待付款订单。  
  - 每行提供“整单发货”“分次发货”按钮：  
    - 整单发货：输入快递公司、运单号，一次性发完所有明细数量。  
    - 分次发货：为每条明细填写本次发货数量，允许部分发货。  
  - 发货后订单状态进入 `DELIVERING`，并记录发货单；分次发货会累计到同一订单下的发货记录。

- **后端接口**
  - `GET /api/admin/orders?status=...` → `AdminOrderController.listOrders()`  
    - 复用订单查询，前端在发货页按状态筛选（`PENDING_SHIPMENT` / `DELIVERING` / `PENDING_PAYMENT` 赊销场景 / 全部）。  
  - `POST /api/admin/orders/{orderId}/ship` → `AdminOrderController.shipOrder()`  
    - 调用 `ShipmentService.shipOrder` 执行整单发货，内部校验库存、信用等级（支持高等级先发货后付款），创建发货单并更新订单状态为 `DELIVERING`。  
  - `POST /api/admin/orders/{orderId}/ship/partial` → `AdminOrderController.shipOrderPartially()`  
    - 调用 `ShipmentService.shipOrderPartially` 执行分次发货，仅处理 `shipQuantity > 0` 的明细，校验剩余可发数量与库存，更新发货/订单进度。

- **前端实现**
  - 文件：`src/pages/admin/AdminLayout.tsx`  
  - 左侧菜单“发货管理”已启用；切换后展示发货专用订单列表：  
    - 状态筛选：待发货 / 配送中 / 待付款（允许赊销）/ 全部。  
    - 表格列同订单管理，并额外提供操作列：查看详情 / 整单发货 / 分次发货。  
    - 整单发货弹窗：输入快递公司、运单号，提交后调用 `/ship`。  
    - 分次发货弹窗：基于订单明细展示“订购数量 / 已发货 / 本次发货数量（可编辑）”，提交后调用 `/ship/partial`。  
  - 详情弹窗沿用订单管理的样式，方便在发货前快速查看订单的明细与历史发货记录。

**迁移状态**：✅ 管理员端“发货管理”已完成迁移，整单/分次发货的业务校验与状态流转与 JavaFX 行为一致；其余模块（库存 / 采购 / 客户 / 供应商 / 书目）仍在计划中。

#### 5.3 库存管理（showInventoryManagement / loadInventory / adjustInventory）

- **旧逻辑**：`AdminView.showInventoryManagement()`  
  - 展示所有库存记录：书号 / 当前库存数量 / 安全库存 / 库位。  
  - 安全库存列可直接编辑，提交后调用 `InventoryDao.updateSafetyStock` 并执行 `checkAndCreateLowStockRecord`：当库存低于安全库存时自动生成缺书记录（source=LOW_STOCK, status=PENDING）。  
  - 操作列提供 “+10 / -10” 按钮，内部调用 `adjustInventory(bookId, delta)`：  
    - delta>0 → `InventoryDao.increaseQuantity`  
    - delta<0 → `InventoryDao.decreaseQuantity`  
    - 调整后同样调用 `checkAndCreateLowStockRecord`。

- **后端接口**
  - `GET /api/admin/inventory` → `AdminInventoryController.list()`  
    - 复用 `InventoryDao.findAll()` 返回全部库存记录。  
  - `POST /api/admin/inventory/{bookId}/safety-stock` → `AdminInventoryController.updateSafetyStock()`  
    - 参数：`{ safetyStock: number }`，校验为非负整数；调用 `InventoryDao.updateSafetyStock`，随后执行 `checkAndCreateLowStockRecord`。  
  - `POST /api/admin/inventory/{bookId}/adjust` → `AdminInventoryController.adjustInventory()`  
    - 参数：`{ delta: number }`，delta>0 调 `increaseQuantity`，delta<0 调 `decreaseQuantity`（不足时返回错误）；随后执行 `checkAndCreateLowStockRecord`。  
  - 私有方法 `checkAndCreateLowStockRecord` 直接复刻 JavaFX 中逻辑：使用 `InventoryDao` 与 `OutOfStockRecordDao.insert` 生成/累加缺书记录，保证与桌面端行为一致。

- **前端实现**
  - 文件：`src/pages/admin/AdminLayout.tsx`  
  - 左侧菜单“库存管理”已启用；切换后展示库存表格：  
    - 列：书号 / 库存数量 / 安全库存（`InputNumber` 可编辑）/ 库位 / 操作。  
    - 安全库存编辑：输入后在前端先更新本行状态，`onBlur` 时调用 `/admin/inventory/{bookId}/safety-stock`，成功后刷新列表。  
    - 操作列：`+10` / `-10` 按钮调用 `/admin/inventory/{bookId}/adjust`，并在成功后重新加载库存。  
  - 与 JavaFX 一样，所有调整都会自动触发缺书记录的生成/累加逻辑。

**迁移状态**：✅ 管理员端“库存管理”已完成迁移，库存调整与缺书记录联动规则与原 JavaFX 完全一致；其余模块（采购 / 客户 / 供应商 / 书目）继续排期迁移中。

#### 5.4 采购管理（showPurchaseManagement 等）

- **旧逻辑**：`AdminView.showPurchaseManagement()`  
  - Tab1「缺书记录」：展示 `OutOfStockRecordDao.findByStatus('PENDING')`，支持多选，按钮“根据选中缺书生成采购单”调用 `showCreatePurchaseFromOutOfStockDialog`，进而使用 `PurchaseService.createPurchaseOrderFromOutOfStock` 生成采购单，并将缺书记录状态更新为 `PURCHASING`。  
  - 按钮“添加缺书记录”`showAddOutOfStockDialog()`：仅填写书号与需求数量，创建一条 `OutOfStockRecord`（source=MANUAL, status=PENDING, priority=1），由 `OutOfStockRecordDao.insert` 负责处理同书同状态记录的累加逻辑。  
  - Tab2「顾客缺书登记」：基于 `CustomerOutOfStockRequestDao.findPendingUnpaid()` 展示未付款且待处理的顾客缺书登记，按行按钮“生成缺书记录/不生成”：  
    - 生成：插入 `OutOfStockRecord`（source=CUSTOMER_REQUEST），登记缺口量和优先级，并将登记标记为 `ACCEPTED`，订单状态从 `OUT_OF_STOCK_PENDING` 改为 `PENDING_PAYMENT`。  
    - 不生成：仅将登记标记为 `REJECTED`，订单状态从 `OUT_OF_STOCK_PENDING` 改为 `CANCELLED`。  
  - Tab3「采购单」：展示所有采购单，按钮“到货”调用 `PurchaseService.receiveGoods`：  
    - 为每条采购明细增加库存；  
    - 安全地把关联缺书记录状态标记为 `COMPLETED`（内部可能合并记录，避免唯一约束冲突）；  
    - 将采购单状态更新为 `COMPLETED`。

- **后端接口**
  - `GET /api/admin/purchase/out-of-stock` → `AdminPurchaseController.listPendingOutOfStock()`  
    - 复用 `OutOfStockRecordDao.findByStatus('PENDING')`，用于 Tab1 缺书记录表。  
  - `GET /api/admin/purchase/customer-requests` → `AdminPurchaseController.listCustomerRequests()`  
    - 复用 `CustomerOutOfStockRequestDao.findPendingUnpaid()`，用于 Tab2 顾客缺书登记表。  
  - `POST /api/admin/purchase/customer-requests/{requestId}/accept` → `AdminPurchaseController.acceptCustomerRequest()`  
    - 查找指定登记（通过遍历 pending 列表模拟 `findById`），插入一条 `OutOfStockRecord`（source=CUSTOMER_REQUEST），  
      调 `CustomerOutOfStockRequestDao.updateProcessedStatus(...,'ACCEPTED', recordId)`，调用 `SalesOrderDao.updateStatusAndPaymentTime(...,'PENDING_PAYMENT',null)`，  
      并额外写入一条 `CustomerNotification`（type=OUT_OF_STOCK，标题“缺书登记处理结果”，内容“您的订单（xxx）的缺货登记已通过，请抓紧付款。”），供 Web 端“消息通知”列表与弹窗展示。  
  - `POST /api/admin/purchase/customer-requests/{requestId}/reject` → `AdminPurchaseController.rejectCustomerRequest()`  
    - 类似 Accept 分支，但只更新 processed_status 为 `REJECTED`，同时将订单状态改为 `CANCELLED`，  
      并写入一条 `CustomerNotification`，内容“您的订单（xxx）的缺货登记未通过，订单已取消。”。  
  - `POST /api/admin/purchase/orders/from-out-of-stock` → `AdminPurchaseController.createPurchaseFromOutOfStock()`  
    - 调用 `PurchaseService.createPurchaseOrderFromOutOfStock`，完全复用原有供应商价格匹配、金额计算与状态流转逻辑，返回新建采购单对象。  
  - `GET /api/admin/purchase/orders` / `GET /api/admin/purchase/orders/{id}`  
    - 使用 `PurchaseOrderDao.findAll` / `findItemsByOrderId` 提供采购单列表与详情。  
  - `POST /api/admin/purchase/orders/{id}/receive` → `AdminPurchaseController.receiveGoods()`  
    - 调用 `PurchaseService.receiveGoods` 完成到货处理（库存入库 + 缺书记录完成 + 采购单完成）。
  - `POST /api/admin/purchase/out-of-stock` → `AdminPurchaseController.addOutOfStockRecord()`  
    - 请求体：`{ bookId, requiredQuantity, priority? }`；  
    - 校验书号非空、需求数量为正整数，source 固定为 `MANUAL`，status 固定为 `PENDING`，priority 默认 1；  
    - 调用 `OutOfStockRecordDao.insert` 写入/累加缺书记录，与 JavaFX `showAddOutOfStockDialog` 完全对应。

- **前端实现**
  - 文件：`src/pages/admin/AdminLayout.tsx`，菜单“采购管理”已启用，对应一个包含三块区域的页面：  
    - **缺书记录区**：表格展示 `/admin/purchase/out-of-stock`，支持多选行；  
      - 顶部提供“添加缺书记录”按钮：弹出表单仅填书号与需求数量（可选优先级），提交时调用 `POST /admin/purchase/out-of-stock`，成功后提示并刷新缺书记录区；  
      - “根据选中缺书生成采购单”按钮：弹出对话框填写供应商ID、期望到货日期、采购员，提交调用 `/admin/purchase/orders/from-out-of-stock` 并刷新三块数据。  
    - **顾客缺书登记区**：表格展示 `/admin/purchase/customer-requests`，每行有“生成缺书记录/不生成”按钮，分别调用 `/accept` 与 `/reject`，并刷新视图。  
    - **采购单区**：表格展示 `/admin/purchase/orders`，点击采购单号或“明细”打开详情弹窗，表格中“到货”按钮调用 `/orders/{id}/receive` 完成入库与状态更新。  
  - 所有业务分支均直接复用 `PurchaseService`、各 DAO 与原 JavaFX 中的业务规则，仅在交互和布局上做了 Web 端的视觉优化。

**迁移状态**：✅ 管理员端“采购管理”已完成迁移，缺书记录（含**手动添加缺书记录**）→ 采购单 → 到货入库的整条业务链与桌面端保持完全一致；后续将继续维护与优化客户管理 / 供应商管理 / 书目管理模块。

#### 5.5 客户管理（showCustomerManagement / showCreditDialog）

- **旧逻辑**：`AdminView.showCustomerManagement()`  
  - 展示客户列表：客户ID / 用户名 / 真实姓名 / 余额 / 累积消费 / 信用等级ID。  
  - “操作”列提供“调整信用”按钮，弹出 `showCreditDialog`：  
    - 通过 `CustomerDao.updateCreditLevel(customerId, newLevel)` 将信用等级调整为 1~5 级。  
    - 调整成功后刷新客户列表。

- **后端接口**
  - `GET /api/admin/customers` → `AdminCustomerController.listCustomers()`  
    - 直接复用 `CustomerDao.findAll()`，返回全部客户字段，供前端展示。  
  - `POST /api/admin/customers/{customerId}/credit-level` → `AdminCustomerController.updateCreditLevel()`  
    - 请求体：`{ creditLevelId: number }`，校验范围 1~5；  
    - 调用 `CustomerDao.updateCreditLevel(customerId, level)`，成功后返回 200，无额外计算逻辑。

- **前端实现**
  - 文件：`src/pages/admin/AdminLayout.tsx`  
  - 左侧菜单“客户管理”已启用；切换后展示客户表格：  
    - 列：客户ID / 用户名 / 真实姓名 / 余额 / 累积消费 / 信用等级ID / 操作。  
    - “操作”列使用 1~5 级按钮模拟原来的信用等级选择：  
      - 当前等级按钮使用 `type="primary"` 高亮，点击任何一个等级按钮都会调用 `/admin/customers/{id}/credit-level` 并在成功后刷新列表。  
  - 不额外更改余额或消费等字段，只做信用等级调整，业务行为与 JavaFX 保持一致。

**迁移状态**：✅ 管理员端“客户管理”已完成迁移；管理员现在可以在 Web 后台直接查看客户列表并调整信用等级。供应商管理 / 书目管理将继续按原 AdminView 功能逐步迁移。

#### 5.6 供应商管理（showSupplierManagement / showAddSupplierDialog）

- **旧逻辑**：`AdminView.showSupplierManagement()`  
  - 展示供应商列表：供应商ID / 名称 / 联系人 / 电话 / 状态。  
  - 按钮“添加供应商”弹出对话框 `showAddSupplierDialog`，填写名称、联系人、电话、邮箱、地址，默认合作状态 `ACTIVE`，调用 `SupplierDao.insert` 新增记录并刷新列表。  
  - **注意**：JavaFX 版本中，供应商的供货关系管理在“书目管理 → 供货关系”对话框中实现（按书目维度管理）；Web 端新增了“按供应商维度查看和编辑供货清单”的功能，便于管理员从供应商角度管理供货关系。

- **后端接口**
  - `GET /api/admin/suppliers` → `AdminSupplierController.listSuppliers()`  
    - 复用 `SupplierDao.findAll()` 返回所有供应商字段。  
  - `POST /api/admin/suppliers` → `AdminSupplierController.addSupplier()`  
    - 请求体：`{ supplierName, contactPerson, phone, email, address, paymentTerms }`，  
      校验名称非空，补充 `cooperationStatus='ACTIVE'`，调用 `SupplierDao.insert` 新增供应商。
  - `GET /api/admin/suppliers/{supplierId}/supplies` → `AdminSupplierController.getSupplierSupplies()`  
    - 调用 `SupplyDao.findBySupplierId(supplierId)` 获取该供应商的所有供货关系；  
    - 对每条供货关系，通过 `BookDao.findById` 查询书目信息（书名、ISBN、出版社、定价）；  
    - 返回 `SupplyDetailResp` 列表，包含供货关系字段（供价、提前期、是否主供货商）与书目信息。
  - `POST /api/admin/suppliers/{supplierId}/supplies` → `AdminSupplierController.addSupply()`  
    - 请求体：`{ bookId, supplyPrice?, leadTimeDays?, primary? }`；  
    - 校验书号非空并检查书目是否存在；调用 `SupplyDao.insert` 新增供货关系（如标记为主供货商，会自动将该书其他供货关系的 `is_primary` 置为 0）。
  - `PUT /api/admin/suppliers/{supplierId}/supplies/{bookId}` → `AdminSupplierController.updateSupply()`  
    - 请求体：`{ supplyPrice?, leadTimeDays?, primary? }`；  
    - 调用 `SupplyDao.update` 更新供货价、提前期与主供货商标记；如设置为主供货商，会先将该书其他供货关系的 `is_primary` 置为 0。
  - `DELETE /api/admin/suppliers/{supplierId}/supplies/{bookId}` → `AdminSupplierController.deleteSupply()`  
    - 调用 `SupplyDao.delete` 删除指定供货关系。

- **前端实现**
  - 文件：`src/pages/admin/AdminLayout.tsx`，菜单“供应商管理”已启用：  
    - 表格列：供应商ID / 名称 / 联系人 / 电话 / 邮箱 / 地址 / 状态 / 操作。  
    - 顶部“添加供应商”按钮弹出对话框，输入名称、联系人、电话、邮箱、地址、结算条款，提交时调用 `/admin/suppliers`，成功后关闭弹窗并刷新列表。  
    - **供货清单管理**（新增功能）：  
      - 每行供应商提供“查看供货清单”按钮，点击后打开供货清单弹窗。  
      - 供货清单弹窗展示该供应商的所有供货关系表格：  
        - 列：书号 / 书名 / ISBN / 出版社 / 书目定价 / 供货价 / 提前期(天) / 是否主供货商 / 操作。  
        - 支持行内编辑：点击“编辑”按钮后，供货价、提前期、是否主供货商变为可编辑状态（`InputNumber` / `Checkbox`），点击“保存”调用 `PUT /suppliers/{supplierId}/supplies/{bookId}` 更新，点击“取消”恢复原值。  
        - 支持删除：每行提供“删除”按钮，带二次确认（`Popconfirm`），确认后调用 `DELETE /suppliers/{supplierId}/supplies/{bookId}`。  
        - 顶部“添加供货关系”按钮：弹出表单对话框，输入书号、供货价（可选）、提前期（可选）、是否设为主供货商，提交时调用 `POST /suppliers/{supplierId}/supplies`，成功后刷新供货清单表格。  
      - 所有操作成功后都会自动刷新供货清单列表，保证数据一致性。

**迁移状态**：✅ 管理员端“供应商管理”已完成迁移；管理员可在 Web 后台维护供应商基础信息，并**按供应商维度查看和编辑供货清单**（供价、提前期、主供货商标记），功能比 JavaFX 版本更完善（JavaFX 仅支持在书目管理中按书目维度管理供货关系）。

#### 5.7 书目管理（showBookManagement / showAddBookDialog）

- **旧逻辑**：`AdminView.showBookManagement()` 及其子对话框：  
  - 主界面表格展示书目列表：书号 / 书名 / ISBN / 出版社 / 定价 / 状态。  
  - “添加书目”对话框 `showAddBookDialog`：填写书号、ISBN、书名、出版社、定价、封面URL、目录、初始库存、安全库存，  
    使用 `BookDao.insert` 新增书目，再用 `InventoryDao.insert` 初始化 `inventory` 记录。  
  - “作者/关键字”对话框：基于 `AuthorDao` / `KeywordDao` / `BookAuthorKeywordDao`，实现作者顺序维护、关键字增删。  
  - “供货关系”对话框：基于 `SupplyDao` / `SupplierDao`，为每本书维护多个供货商的进价、提前期与主供货商标记。  
  - “编辑详情”对话框：允许修改书目的 ISBN、书名、出版社、版次、定价、状态、封面URL 与目录等基础信息。

- **后端接口**
  - `GET /api/admin/books` → `AdminBookController.listBooks()`  
    - 复用 `BookDao.findAll()` 返回全部书目字段。  
  - `POST /api/admin/books` → `AdminBookController.addBook()`  
    - 请求体：`{ bookId, isbn, title, publisher, price, coverImageUrl, catalog, initQuantity, safetyStock }`；  
    - 校验书号/书名非空、价格非负、初始库存及安全库存为非负整数；  
    - 调用 `BookDao.insert` 写入书目；随后构造 `Inventory` 调用 `InventoryDao.insert` 初始化库存，与 JavaFX 一致。  
  - `GET /api/admin/books/{bookId}` → `AdminBookController.getBookDetail()`  
    - 返回 `BookDetailResp`：`book + authors + keywords + supplies`，用于 Web 端“编辑详情”弹窗一次性加载所有元数据。  
  - `PUT /api/admin/books/{bookId}` → `AdminBookController.updateBook()`  
    - 对应 JavaFX “编辑详情”，允许更新 ISBN / 书名 / 出版社 / 版次 / 定价 / 状态 / 封面URL / 目录，调用 `BookDao.update`。

- **作者 / 关键字维护接口**
  - `GET /api/admin/books/{bookId}/authors` → `listAuthors()`  
    - 复用 `AuthorDao.findByBookId(bookId)`，返回携带 `author_order` 的作者列表。  
  - `POST /api/admin/books/{bookId}/authors` → `addAuthorToBook()`  
    - 请求体：`{ authorId?, authorName, nationality?, biography?, authorOrder? }`；  
    - 若 `authorId` 为空，则先用 `AuthorDao.insert` 创建作者，再用 `BookAuthorKeywordDao.addBookAuthor` 关联书目与作者，并写入顺序；  
    - 若 `authorId` 已存在，则只建立关联。  
  - `PUT /api/admin/books/{bookId}/authors/{authorId}` → `updateAuthorForBook()`  
    - 可同时修改作者基础信息（`AuthorDao.update`）与作者顺序（`BookAuthorKeywordDao.updateBookAuthorOrder`）。  
  - `DELETE /api/admin/books/{bookId}/authors/{authorId}` → `removeAuthorFromBook()`  
    - 调用 `BookAuthorKeywordDao.removeBookAuthor` 解除某书与指定作者的关联。  
  - `GET /api/admin/books/{bookId}/keywords` → `listKeywords()`  
    - 复用 `KeywordDao.findByBookId(bookId)`。  
  - `POST /api/admin/books/{bookId}/keywords` → `addKeywordToBook()`  
    - 请求体：`{ keywordId?, keywordText }`；若 `keywordId` 为空则先 `KeywordDao.insert` 再 `BookAuthorKeywordDao.addBookKeyword`。  
  - `PUT /api/admin/books/{bookId}/keywords/{keywordId}` → `updateKeyword()`  
    - 更新关键字文本，内部调 `KeywordDao.update`。  
  - `DELETE /api/admin/books/{bookId}/keywords/{keywordId}` → `removeKeywordFromBook()`  
    - 调用 `BookAuthorKeywordDao.removeBookKeyword` 解除关联。

- **供货关系维护接口**
  - `GET /api/admin/books/{bookId}/supplies` → `listSupplies()`  
    - 使用 `SupplyDao.findByBookId(bookId)` 查询该书所有供货记录。  
  - `POST /api/admin/books/{bookId}/supplies` → `addSupply()`  
    - 请求体：`{ supplierId, supplyPrice?, leadTimeDays?, primary }`；  
    - 复用 `SupplyDao.insert`：当标记为主供货商时，会把该书其他供货关系的 `is_primary` 先置为 0，再插入新记录。  
  - `PUT /api/admin/books/{bookId}/supplies/{supplierId}` → `updateSupply()`  
    - 请求体：`{ supplyPrice?, leadTimeDays?, primary? }`；调用 `SupplyDao.update` 按 (supplierId, bookId) 更新进价、提前期与主供货商标记。  
  - `DELETE /api/admin/books/{bookId}/supplies/{supplierId}` → `deleteSupply()`  
    - 调用 `SupplyDao.delete` 删除该供应商与该书之间的供货关系。

- **前端实现**
  - 文件：`src/pages/admin/AdminLayout.tsx`，菜单“书目管理”已启用：  
    - 表格列：书号 / 书名 / ISBN / 出版社 / 定价 / 状态，数据来自 `/admin/books`；  
      点击“书号”会打开 Web 版的“编辑详情”弹窗，对应 JavaFX 的详情编辑界面。  
    - “添加书目”弹窗：表单项与 JavaFX 对话框保持一致（包括初始库存和安全库存），提交时调用 `/admin/books`，成功后刷新列表。  
    - “编辑详情”弹窗结构：  
      - 顶部“基本信息”区：可编辑书名 / ISBN / 出版社 / 版次 / 定价 / 状态 / 封面URL / 目录，点击“保存基本信息”调用 `PUT /admin/books/{bookId}`。  
      - “作者”区：表格展示当前作者（含顺序），支持：  
        - 在行内用 `InputNumber` 修改作者顺序（调用 `PUT /authors/{authorId}`）；  
        - 通过表单新增作者姓名/国籍与顺序（调用 `POST /authors`）；  
        - “移除”按钮删除作者与本书的关联（`DELETE /authors/{authorId}`）。  
      - “关键字”区：表格展示关键字列表，支持新增与移除（分别对应 `POST /keywords` 与 `DELETE /keywords/{id}`）。  
      - “供货关系”区：表格展示供应商ID / 供货价 / 提前期 / 是否主供货商，支持新增和删除供货记录（`POST /supplies` / `DELETE /supplies/{supplierId}`），新增时可勾选“设为主供货商”，触发与 JavaFX 相同的主供货商唯一性逻辑。

**迁移状态**：✅ 管理员端“书目管理”已完成**完整迁移**：包含列表、添加书目（含库存初始化）、作者/关键字维护、供货关系维护以及详情编辑，行为与 JavaFX 版本保持一致，仅在交互与布局上做了 Web 端的视觉优化。

---

---

### 六、迁移进度小结

#### ✅ 已完成迁移的核心功能

**顾客端（CustomerView → `/customer`）：**
- ✅ 登录/注册
- ✅ 书目浏览与搜索（含作者/关键字模糊匹配）
- ✅ 书目详情查看
- ✅ 购物车与折扣计算
- ✅ 下单流程（含缺书检查与缺书登记决策）
- ✅ “我的订单”列表、详情、付款、收货确认
- ✅ 收货地址管理（列表、新增、设为默认、删除）
- ✅ 账户充值
- ✅ 个人信息修改
- ✅ 消息通知列表（含缺书登记处理结果通知）

**管理员端（AdminView → `/admin`）：**
- ✅ 订单管理（列表、详情、状态筛选）
- ✅ 发货管理（整单发货、分次发货）
- ✅ 库存管理（列表、安全库存编辑、数量调整、自动生成缺书记录）
- ✅ 采购管理（缺书记录列表、手动添加缺书记录、顾客缺书登记处理、采购单生成与到货）
- ✅ 客户管理（列表、信用等级调整）
- ✅ 供应商管理（列表、新增、供货清单查看与编辑）
- ✅ 书目管理（列表、新增、详情编辑、作者/关键字维护、供货关系维护）

#### ✅ 已迁移的后台业务逻辑（不易测试但已确认）

1. **信用等级自动升级**：
   - 逻辑位置：`OrderService.payOrder()` → `checkAndUpgradeCreditLevel()`。
   - 升级规则：根据累积消费自动升级（二级≥500元、三级≥2000元、四级≥5000元、五级≥10000元）。
   - Web 端执行：顾客在“我的订单”中点击“付款”时，`CustomerOrderController.pay()` → `OrderService.payOrder()` 会自动检查并升级信用等级，无需前端额外处理。
   - **状态**：✅ 已迁移，逻辑与 JavaFX 完全一致。

2. **累积消费更新**：
   - 逻辑位置：`OrderService.payOrder()` → `CustomerDao.addTotalConsumption()`。
   - Web 端执行：付款时自动更新，为信用等级自动升级提供数据基础。
   - **状态**：✅ 已迁移。

3. **缺书登记自动生成**：
   - 逻辑位置：`OrderService.payOrder()` 中，当订单原状态为 `OUT_OF_STOCK_PENDING` 时，自动为未处理的顾客缺书登记生成正式缺书记录。
   - Web 端执行：顾客在缺书登记决策中选择“付款并生成缺书记录”时，会调用 `OrderService.payOrder()`，内部自动处理。
   - **状态**：✅ 已迁移。

4. **库存低于安全库存时自动生成缺书记录**：
   - 逻辑位置：`AdminInventoryController.updateSafetyStock()` / `adjustInventory()` → `checkAndCreateLowStockRecord()`。
   - Web 端执行：管理员在库存管理中调整安全库存或库存数量时自动触发。
   - **状态**：✅ 已迁移。

#### ⚠️ 待完善的功能（非核心，不影响主要业务流程）

1. **收货地址在下单流程中的选择与快照**：
   - 当前：自动选择默认地址或唯一地址。
   - 待完善：提供弹窗式地址选择器，允许顾客在下单时从多个地址中选择。

#### 📊 迁移完成度统计

- **顾客端功能**：约 **95%** 已完成（仅缺地址选择器）
- **管理员端功能**：**100%** 已完成
- **后台业务逻辑**：**100%** 已迁移（包括信用等级自动升级、累积消费更新、缺书登记自动生成等）

---

如需了解某一块迁移细节（例如“缺书登记流程在 Web 端如何实现”），可以直接在对应 JavaFX 方法名基础上询问，我会补充更细的映射说明与实现情况。

